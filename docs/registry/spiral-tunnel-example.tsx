import { SpiralTunnel, type SpiralTunnelProps } from '@paper-design/shaders-react';

export function SpiralTunnelExample(props: SpiralTunnelProps) {
  return <SpiralTunnel style={{ position: 'fixed', width: '100%', height: '100%' }} {...props} />;
}
